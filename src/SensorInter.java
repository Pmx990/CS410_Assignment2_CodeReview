/*
* The interface of the adapters.
* */
public interface SensorInter {
    //read(), report(), and type() functions are shared by all three sensors
    //interface should catpure this
    void uiPart(UiFunction uf);
    }


